import styled from 'styled-components';
import Wrapper from 'components/Wrapper';
import stylesFlex from 'components/stylesFlex';

const BannnerWrapper = styled(Wrapper)`
  ${stylesFlex};
  padding-top: 100px;
`;

export default BannnerWrapper;
